---
import { statusData } from "../data/data.ts"
---
<div class="statusBox">
  <h3 id="statusH3">Currently</h3>
  <ul >
    {statusData.map((site) => (
      <li>{site.name}: {site.status}</li>
    ))}
  </ul>
</div>
